<script lang="ts">
	export const csr = true;
	const domain = "openbooru.org";
	const dmca_email = `dmca@${domain}`;
	const support_email = `support@${domain}`;
	const gdpr_email = `gdpr@${domain}`;
</script>

<h1>Contact Us</h1>

If you want to contact us, you can reach us through the following emails:

<div>
	<br />
	<a href={`mailto:dmac@${domain}`}>
		dmca@openbooru.org
	</a>
</div>
If you want to have your content removed from the site.<br/>
Note: Although we comply with requests and blacklist content, we do not host the content so you'll have file another request with the post's source as well

<div>
	<br/>
	<a href={`mailto:gdpr@${domain}`}>
		gdpr@openbooru.org
	</a>
</div>

If you need support on any issues involving GDPR such as data deletion or requesting your data.

<div>
	<br />
	<a href={`mailto:support@${domain}`}>
		support@openbooru.org
	</a>
</div>
If you need any other help more general help involving the site. This can be anything from suggestions to inqueries, we'll be happy to respond.
